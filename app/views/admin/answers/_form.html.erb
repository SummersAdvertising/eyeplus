<%= form_for(new_admin_question_answer_path(@question, @question.answers.build)) do |f| %>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
  adsfasdfasdfsa
<% end %>
