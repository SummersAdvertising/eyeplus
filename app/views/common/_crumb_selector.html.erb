<div id="bread-crumbs"><a href="<%=root_path%>">首頁</a> > 
    <%if request.original_fullpath.include?("/introduction")%>
        <a href="<%=static_pages_introduction_path%>">診所介紹</a>
    <% end %>
    <%if request.original_fullpath.include?("/services")%>
        <a href="<%=static_pages_services_path%>">服務項目</a>
    <% end %>
    <%if request.original_fullpath.include?("/staff")%>
        <a href="<%=static_pages_staff_path%>">醫師陣容</a>
    <% end %>
    <%if request.original_fullpath.include?("/questions")%>
        <a href="<%=questions_path%>">醫師答客問</a>
    <% end %>
    <%if request.original_fullpath.include?("/news")%>
        <a href="<%=questions_path%>">最新消息</a>
    <% end %>


    <%if request.original_fullpath.include?("/boards/")%>
        <a href="<%=board_path(@first_board)%>">駐診醫師專欄</a> >    
        <%if request.original_fullpath.include?("/boards/"+@first_board.id.to_s())%>
            <a href="<%=board_path(@first_board)%>"><%=@first_board.name%>
        <% end %>
        <%if request.original_fullpath.include?("/boards/"+@second_board.id.to_s())%>
            <a href="<%=board_path(@second_board)%>"><%=@second_board.name%>
        <% end %>
        <%if request.original_fullpath.include?("/boards/"+@third_board.id.to_s())%>
            <a href="<%=board_path(@third_board)%>"><%=@third_board.name%>
        <% end %>
        <%if request.original_fullpath.include?("/boards/"+@fourth_board.id.to_s())%>
            <a href="<%=board_path(@fourth_board)%>"><%=@fourth_board.name%>
        <% end %>
    <% end %>
</div>