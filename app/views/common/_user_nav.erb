<div class="user_navigation">
  <% if user_signed_in? %>
      Hi! <%= current_user.email %>
      <%= link_to("Sign out", destroy_user_session_path, :method => :delete )%>
      <% if (current_user) && (current_user.is_admin?) && (url_for(:only_path => true).include? 'questions') %>
        <%= link_to("Admin Panel", admin_questions_path )%>
      <% end %>
      <% if (current_user) && (current_user.is_admin?) && (url_for(:only_path => true).include? 'posts') %>
        <%= link_to("Admin Panel", admin_posts_path )%>
      <% end %>
      <% if (current_user) && (current_user.is_admin?) && (url_for(:only_path => true).include? 'boards') %>
        <%= link_to("Admin Panel", admin_boards_path )%>
      <% end %>
<!--       <% if (current_user) && (current_user.is_admin?) && (url_for(:only_path => true).include? 'facebooks') %>
        <%= link_to("Admin Panel", admin_facebooks_path )%>
      <% end %>
      <% if (current_user) && (current_user.is_admin?) && (url_for(:only_path => true).include? 'default_facebooks') %>
        <%= link_to("Admin Panel", admin_default_facebooks_path )%>
      <% end %> -->
  <% else %>
      <%= link_to "Sign in", new_user_session_path %>
      <%= link_to "Sign up", new_user_registration_path %>
  <% end %>
</div>